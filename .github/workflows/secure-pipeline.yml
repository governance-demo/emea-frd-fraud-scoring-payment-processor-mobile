# governance-demo per-repo secure-pipeline template.
# The uses: line at 18 is the tracked action reference for the fleet record.
# Line numbering is load-bearing; do not renumber.

name: secure-pipeline

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout source
        uses: internal/actions/build@5192aa0104d59dc4cdf9ed87ac65d06ae6ef7f8b
